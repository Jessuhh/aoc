import { getInput } from "utils";

const input = getInput("2022/13/test.txt", "\n\n");
// const input = getInput("2022/13/input.txt", "\n\n");

function stringTolist(input: string) {
    const list = [];
    
}

const part1 = () => {
    const pairs = input;
    const sumOfIndices = 0;
    pairs.forEach((pair, index) => {
        const packets = pair.split("\n");
        //

    })
    return sumOfIndices;
}

console.log(part1());
